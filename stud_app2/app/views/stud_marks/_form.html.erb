<%= form_for(@stud_mark) do |f| %>
  <% if @stud_mark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stud_mark.errors.count, "error") %> prohibited this stud_mark from being saved:</h2>

      <ul>
      <% @stud_mark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_id %><br>
    <%= f.text_field :student_id %>
  </div>
  <div class="field">
    <%= f.label :subject_id %><br>
    <%= f.text_field :subject_id %>
  </div>
  <div class="field">
    <%= f.label :mark %><br>
    <%= f.number_field :mark %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
